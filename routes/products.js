module.exports = function registerProductRoutes(app, { getPool, sql, requireUser, requireAdmin, logAction, actorName, sendInternalError }) {
  app.get("/api/products", async (req, res) => { try { const result = await (await getPool()).request().query("SELECT ProductID AS id, Name AS name, Type AS type, Brand AS brand, Price AS price, Stocks AS stocks, HorsePower AS horsepower, Image AS image FROM tblProduct ORDER BY ProductID DESC"); res.json(result.recordset); } catch (error) { sendInternalError(res, error, "Request failed"); } });
  app.post("/api/products", requireUser, requireAdmin, async (req, res) => { const { name, type, brand, price, stocks, horsepower, image } = req.body; if (!name || !type || !brand || price === undefined || stocks === undefined || !horsepower) return res.status(400).json({ message: "Missing required product fields." }); try { const result = await (await getPool()).request().input("Name", sql.NVarChar(100), name).input("Type", sql.NVarChar(100), type).input("Brand", sql.NVarChar(100), brand).input("Price", sql.Decimal(10, 2), Number(price)).input("Stocks", sql.Int, Number(stocks)).input("Horsepower", sql.NVarChar(50), horsepower).input("Image", sql.NVarChar(sql.MAX), image || null).query("INSERT INTO tblProduct (Name, Type, Brand, Price, Stocks, HorsePower, Image) OUTPUT INSERTED.ProductID AS id, INSERTED.Name AS name, INSERTED.Type AS type, INSERTED.Brand AS brand, INSERTED.Price AS price, INSERTED.Stocks AS stocks, INSERTED.HorsePower AS horsepower, INSERTED.Image AS image VALUES (@Name, @Type, @Brand, @Price, @Stocks, @Horsepower, @Image)"); await logAction(`Created product ${name}`, actorName(req), "tblProduct", result.recordset[0].id); res.status(201).json(result.recordset[0]); } catch (error) { sendInternalError(res, error, "Request failed"); } });
  app.put("/api/products/:id", requireUser, requireAdmin, async (req, res) => { const { name, type, brand, price, stocks, horsepower, image } = req.body; if (!name || !type || !brand || price === undefined || stocks === undefined || !horsepower) return res.status(400).json({ message: "Missing required product fields." }); try { const result = await (await getPool()).request().input("Id", sql.Int, Number(req.params.id)).input("Name", sql.NVarChar(100), name).input("Type", sql.NVarChar(100), type).input("Brand", sql.NVarChar(100), brand).input("Price", sql.Decimal(10, 2), Number(price)).input("Stocks", sql.Int, Number(stocks)).input("Horsepower", sql.NVarChar(50), horsepower).input("Image", sql.NVarChar(sql.MAX), image || null).query("UPDATE tblProduct SET Name=@Name, Type=@Type, Brand=@Brand, Price=@Price, Stocks=@Stocks, HorsePower=@Horsepower, Image=@Image OUTPUT INSERTED.ProductID AS id, INSERTED.Name AS name, INSERTED.Type AS type, INSERTED.Brand AS brand, INSERTED.Price AS price, INSERTED.Stocks AS stocks, INSERTED.HorsePower AS horsepower, INSERTED.Image AS image WHERE ProductID=@Id"); if (!result.recordset.length) return res.status(404).json({ message: "Product not found." }); await logAction(`Updated product ${name}`, actorName(req), "tblProduct", req.params.id); res.json(result.recordset[0]); } catch (error) { sendInternalError(res, error, "Request failed"); } });
  app.delete("/api/products/:id", requireUser, requireAdmin, async (req, res) => { try { await (await getPool()).request().input("Id", sql.Int, Number(req.params.id)).query("DELETE FROM tblProduct WHERE ProductID = @Id"); await logAction("Deleted a product", actorName(req), "tblProduct", req.params.id); res.status(204).end(); } catch (error) { sendInternalError(res, error, "Request failed"); } });
};
