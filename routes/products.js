module.exports = function registerProductRoutes(app, { getPool, sql, requireUser, requireAdmin, logAction, actorName, sendInternalError }) {
  app.get("/api/products", async (req, res) => { try { const result = await (await getPool()).request().query("SELECT Id AS id, Name AS name, Type AS type, Brand AS brand, Price AS price, Stocks AS stocks, Horsepower AS horsepower, Image AS image FROM Products ORDER BY Id DESC"); res.json(result.recordset); } catch (error) { sendInternalError(res, error, "Request failed"); } });
  app.post("/api/products", requireUser, requireAdmin, async (req, res) => { const { name, type, brand, price, stocks, horsepower, image } = req.body; if (!name || !type || !brand || price === undefined || stocks === undefined || !horsepower) return res.status(400).json({ message: "Missing required product fields." }); try { const result = await (await getPool()).request().input("Name", sql.NVarChar(100), name).input("Type", sql.NVarChar(100), type).input("Brand", sql.NVarChar(100), brand).input("Price", sql.Decimal(10, 2), Number(price)).input("Stocks", sql.Int, Number(stocks)).input("Horsepower", sql.NVarChar(50), horsepower).input("Image", sql.NVarChar(sql.MAX), image || null).query("INSERT INTO Products (Name, Type, Brand, Price, Stocks, Horsepower, Image) OUTPUT INSERTED.Id AS id, INSERTED.Name AS name, INSERTED.Type AS type, INSERTED.Brand AS brand, INSERTED.Price AS price, INSERTED.Stocks AS stocks, INSERTED.Horsepower AS horsepower, INSERTED.Image AS image VALUES (@Name, @Type, @Brand, @Price, @Stocks, @Horsepower, @Image)"); await logAction(`Created product ${name}`, actorName(req), "Products", result.recordset[0].id); res.status(201).json(result.recordset[0]); } catch (error) { sendInternalError(res, error, "Request failed"); } });
  app.put("/api/products/:id", requireUser, requireAdmin, async (req, res) => { const { name, type, brand, price, stocks, horsepower, image } = req.body; if (!name || !type || !brand || price === undefined || stocks === undefined || !horsepower) return res.status(400).json({ message: "Missing required product fields." }); try { const result = await (await getPool()).request().input("Id", sql.Int, Number(req.params.id)).input("Name", sql.NVarChar(100), name).input("Type", sql.NVarChar(100), type).input("Brand", sql.NVarChar(100), brand).input("Price", sql.Decimal(10, 2), Number(price)).input("Stocks", sql.Int, Number(stocks)).input("Horsepower", sql.NVarChar(50), horsepower).input("Image", sql.NVarChar(sql.MAX), image || null).query("UPDATE Products SET Name=@Name, Type=@Type, Brand=@Brand, Price=@Price, Stocks=@Stocks, Horsepower=@Horsepower, Image=@Image OUTPUT INSERTED.Id AS id, INSERTED.Name AS name, INSERTED.Type AS type, INSERTED.Brand AS brand, INSERTED.Price AS price, INSERTED.Stocks AS stocks, INSERTED.Horsepower AS horsepower, INSERTED.Image AS image WHERE Id=@Id"); if (!result.recordset.length) return res.status(404).json({ message: "Product not found." }); await logAction(`Updated product ${name}`, actorName(req), "Products", req.params.id); res.json(result.recordset[0]); } catch (error) { sendInternalError(res, error, "Request failed"); } });
  app.delete("/api/products/:id", requireUser, requireAdmin, async (req, res) => { try { await (await getPool()).request().input("Id", sql.Int, Number(req.params.id)).query("DELETE FROM Products WHERE Id = @Id"); await logAction("Deleted a product", actorName(req), "Products", req.params.id); res.status(204).end(); } catch (error) { sendInternalError(res, error, "Request failed"); } });
};
