const { isValidPhilippineMobile } = require("../utils/validation");
const { isPastOrInvalidCalendarDate } = require("../utils/scheduling");
module.exports = function registerBookingRoutes(app, { getPool, sql, requireUser, requireAdmin, logAction, actorName, sendInternalError }) {
  app.get("/api/bookings", requireUser, async (req, res) => { try { const pool = await getPool(); const request = pool.request(); const customerFilter = req.user.role === "customer" ? "WHERE LOWER(b.Email) = LOWER(@Email)" : ""; if (req.user.role === "customer") request.input("Email", sql.NVarChar(150), req.user.email); const result = await request.query(`SELECT b.Id AS id, b.CustomerName AS customer, b.Phone AS phone, b.Email AS email, b.ServiceName AS service, b.Address AS address, CONVERT(varchar(10), b.PreferredDate, 23) AS preferredDate, b.PreferredTime AS preferredTime, b.TotalAmount AS totalAmount, b.Status AS status, t.Name AS technician, CONVERT(varchar(10), s.ScheduleDate, 23) AS scheduleDate, s.ScheduleTime AS scheduleTime FROM Bookings b LEFT JOIN Schedules s ON s.BookingId = b.Id LEFT JOIN Technicians t ON t.Id = s.TechnicianId ${customerFilter} ORDER BY b.Id DESC`); res.json(result.recordset); } catch (error) { sendInternalError(res, error, "Request failed"); } });
  app.post("/api/bookings", requireUser, async (req, res) => { const { customer, phone, email, service, services = [], address, preferredDate, preferredTime } = req.body; const selectedServices = Array.isArray(services) && services.length ? services : String(service || "").split(",").map((item) => item.trim()).filter(Boolean); if (!customer || !selectedServices.length || !address || !preferredDate || !preferredTime) return res.status(400).json({ message: "Missing required booking fields." }); if (phone && !isValidPhilippineMobile(phone)) return res.status(400).json({ message: "Please enter a valid Philippine mobile number." }); if (isPastOrInvalidCalendarDate(preferredDate)) return res.status(400).json({ message: "Preferred date cannot be in the past. Please choose today or a future date." }); try { const pool = await getPool(); const pricedServices = await Promise.all(selectedServices.map(async (selected) => { const id = Number(selected?.id); const result = Number.isInteger(id) && id > 0 ? await pool.request().input("Id", sql.Int, id).query("SELECT TOP 1 Id, Name, Type, Price FROM Services WHERE Id = @Id") : await pool.request().input("Name", sql.NVarChar(100), String(selected?.name || selected).trim()).query("SELECT TOP 1 Id, Name, Type, Price FROM Services WHERE Name = @Name"); return result.recordset[0]; })); if (pricedServices.some((item) => !item)) return res.status(400).json({ message: "One or more selected services are no longer available." }); const serviceLabel = pricedServices.map((item) => `${item.Type || "Uncategorized"} - ${item.Name}`).join(", "); const serviceTotal = pricedServices.reduce((sum, item) => sum + Number(item.Price || 0), 0); const result = await pool.request().input("CustomerName", sql.NVarChar(100), customer).input("Phone", sql.NVarChar(50), phone || "").input("Email", sql.NVarChar(150), email || "").input("ServiceName", sql.NVarChar(500), serviceLabel).input("TotalAmount", sql.Decimal(10, 2), serviceTotal).input("Address", sql.NVarChar(255), address).input("PreferredDate", sql.Date, preferredDate).input("PreferredTime", sql.NVarChar(50), preferredTime || "").query("INSERT INTO Bookings (CustomerName, Phone, Email, ServiceName, TotalAmount, Address, PreferredDate, PreferredTime, Status) OUTPUT INSERTED.Id AS id, INSERTED.CustomerName AS customer, INSERTED.Phone AS phone, INSERTED.Email AS email, INSERTED.ServiceName AS service, INSERTED.TotalAmount AS totalAmount, INSERTED.Address AS address, CONVERT(varchar(10), INSERTED.PreferredDate, 23) AS preferredDate, INSERTED.PreferredTime AS preferredTime, INSERTED.Status AS status VALUES (@CustomerName, @Phone, @Email, @ServiceName, @TotalAmount, @Address, @PreferredDate, @PreferredTime, 'Pending')"); await logAction(`Created booking for ${customer}`, actorName(req), "Bookings", result.recordset[0].id); res.status(201).json(result.recordset[0]); } catch (error) { sendInternalError(res, error, "Request failed"); } });
  app.put("/api/bookings/:id/status", requireUser, requireAdmin, async (req, res) => { const { status } = req.body; if (!status) return res.status(400).json({ message: "Status is required." }); try { const result = await (await getPool()).request().input("Id", sql.Int, Number(req.params.id)).input("Status", sql.NVarChar(50), status).query("UPDATE Bookings SET Status = @Status OUTPUT INSERTED.Id AS id, INSERTED.CustomerName AS customer, INSERTED.ServiceName AS service, INSERTED.Address AS address, INSERTED.Status AS status WHERE Id = @Id"); if (!result.recordset.length) return res.status(404).json({ message: "Booking not found." }); await logAction(`Marked booking ${req.params.id} as ${status}`, actorName(req), "Bookings", req.params.id); res.json(result.recordset[0]); } catch (error) { sendInternalError(res, error, "Request failed"); } });
  app.put("/api/bookings/:id/technician-status", requireUser, async (req, res) => { const { status } = req.body; if (req.user.role !== "technician" && req.user.role !== "admin") return res.status(403).json({ message: "Technician access required." }); if (!status) return res.status(400).json({ message: "Status is required." }); try { const result = await (await getPool()).request().input("Id", sql.Int, Number(req.params.id)).input("Status", sql.NVarChar(50), status).query("UPDATE Bookings SET Status = @Status OUTPUT INSERTED.Id AS id, INSERTED.CustomerName AS customer, INSERTED.ServiceName AS service, INSERTED.Address AS address, INSERTED.Status AS status WHERE Id = @Id"); if (!result.recordset.length) return res.status(404).json({ message: "Booking not found." }); await logAction(`Updated job ${req.params.id} to ${status}`, actorName(req), "Bookings", req.params.id); res.json(result.recordset[0]); } catch (error) { sendInternalError(res, error, "Request failed"); } });
  app.put("/api/bookings/:id/cancel", requireUser, async (req, res) => { if (req.user.role !== "customer") return res.status(403).json({ message: "Customer access required." }); try { const pool = await getPool(); const booking = await pool.request().input("Id", sql.Int, Number(req.params.id)).input("Email", sql.NVarChar(150), req.user.email).query("SELECT Id, Status FROM Bookings WHERE Id = @Id AND LOWER(Email) = LOWER(@Email)"); if (!booking.recordset.length) return res.status(404).json({ message: "Booking not found." }); if (["Completed", "Cancelled"].includes(booking.recordset[0].Status)) return res.status(400).json({ message: "This booking can no longer be cancelled." }); await pool.request().input("Id", sql.Int, Number(req.params.id)).query("DELETE FROM Schedules WHERE BookingId = @Id; UPDATE Bookings SET Status = 'Cancelled' WHERE Id = @Id;"); await logAction(`Cancelled booking ${req.params.id}`, actorName(req), "Bookings", req.params.id); res.json({ id: Number(req.params.id), status: "Cancelled" }); } catch (error) { sendInternalError(res, error, "Request failed"); } });
  app.delete("/api/bookings/:id", requireUser, requireAdmin, async (req, res) => { try { const pool = await getPool(); await pool.request().input("Id", sql.Int, Number(req.params.id)).query("DELETE FROM Schedules WHERE BookingId = @Id; DELETE FROM Bookings WHERE Id = @Id;"); await logAction("Deleted a booking", actorName(req), "Bookings", req.params.id); res.status(204).end(); } catch (error) { sendInternalError(res, error, "Request failed"); } });
};
